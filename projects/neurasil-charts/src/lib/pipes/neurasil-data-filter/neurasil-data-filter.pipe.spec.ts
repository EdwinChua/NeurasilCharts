import { NeurasilDataFilter } from './neurasil-data-filter.pipe';

describe('CartItemStringSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NeurasilDataFilter();
    expect(pipe).toBeTruthy();
  });
});
